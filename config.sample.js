/**
 * Copy to config.js. No API key needed — basemap is OSM/CARTO tiles.
 * Layer URLs must end with /FeatureServer/0 (hosted feature layer index).
 */
window.SHIPS_OF_EXILE_CONFIG = {
  routesLayerUrl: "",
  portsLayerUrl: "",
};
